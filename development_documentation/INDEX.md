# Sciuro Development Documentation

## Phase Status
| Phase | Status | Summary |
|---|---|---|---|---|
| **T2 — Manual Transfer Confirmation (Kanban "Link as Transfer")** | **Completed** | Kanban Review candidates now render the TRANSFER badge from `transferCandidateIds` and expose a "Link as Transfer" action that links the flagged pair and records a manual confirmation for the canonical account pair. After 3 manual confirmations the pair is promoted into `account_pair_confirmation` (enabling Tier 0/Tier 2 auto-linking), while automatic engine links keep immediate pair confirmation and never count toward the manual threshold. `TransferUnmatchedFlagged` now carries `candidateTransactionId` (legacy-event tolerant). Migration `16.sqm` adds `account_pair_manual_confirmation`. New tests: engine event publication, `TransferRepositoryManualLinkTest` (5), `AccountPairConfirmationTest` (4), codec round-trip + legacy tolerance. See [phase-T2-manual-transfer-confirmation/TEST_NOTES.md](phase-T2-manual-transfer-confirmation/TEST_NOTES.md). |
| **T1 — Atomic Audit Writes & Audit Integrity** | **Completed** | Every SQLDelight-backed mutation now commits together with its audit entry in a single `database.transaction { }` (mutation + audit roll back together; `AuditRepository.logMutation()` made synchronous so it can run inside the non-suspending lambda). Applied to TransactionRepository, CashAdjustmentRepository, AccountRepository, CategoryRepository (update/delete previously unaudited), ReconciliationEngine, ObligationRepository, DebtRepository, InvestmentRepository, BudgetRepository, TransferRepository. Removed the non-atomic `withAudit` wrapper. Added `auditIntegrityCheck` query + `AuditRepository.getAuditIntegrityGaps()` surfaced as an "Audit Integrity" card in the Developer Options → Health tab. `AuditAtomicityTest` (4 jvmTest integration tests). |
| **K3 — Scroll Fix & Dashboard Transaction Display** | **Completed** | Fixed Account Detail Screen cannot scroll (restructured from static Column to Box+LazyColumn+SheetList with verticalScroll). Fixed Dashboard content clipping when transaction list+banners exceed viewport height (added verticalScroll to inner SheetList Column). Fixed balance chart to use date-filtered transactions instead of all-time data. Verified filtering data flow is logically correct. |
| **K2 — UX Polish (State-of-the-Art Enhancements)** | **Complete** | Comprehensive UX enhancement across 4 phases: unified haptics, long-press menus, edge-to-edge system bars (Phase 1). Shimmer skeleton loading, predictive back gesture, Material You dynamic color (Phase 2). Biometric confirmations, smart notification actions, Glance widgets (Phase 3). Predictive runway analytics, per-account spending velocity, reusable sparkline component (Phase 4). See [phase-K2-ux-polish/PLAN.md](phase-K2-ux-polish/PLAN.md). |
| **Dev-Tools-Pagination-Hero-Palette** | **Completed** | Ingestion log pagination (50-item pages + "Load More"), mark-as-read (individual + bulk) with `is_read` column + "Show read" toggle, pipeline trace pagination (dynamic limit + "Load More"), HeroPanel palette inheritance (`primary`/`onPrimary` instead of hardcoded black/white across all call sites), PillToggle dark-surface theme awareness |
| A0 - Engineering foundations | Completed | Module structure, Koin DI, detekt/ktlint, CI scaffold, SQLDelight + migrations, `development_documentation/` scaffold, test-tier strategy |
| A1 - Audit Log core | Completed | AuditLog entity, SQLDelight schema, AuditRepository, and Repository-wrapper pattern |
| A2 - Ingestion framework | Completed | `IngestionSource` abstraction, `NotificationSourceAdapter`, and staging buffer |
| A3 - Bank & e-wallet parsers | Completed | CIMB, Maybank, BSN, TNG, GrabPay, Boost, ShopeePay and fixture regression suite |
| A4 - LLM-assisted fallback | Completed | Groq Llama 3 API integration, Opt-in architecture, `SciuroParserPipeline` |
| A5 - Financial taxonomy & data model | Completed | Ledger SQLDelight schemas (Account, Category, TransactionRecord) and Koin Repositories |
| A6 - Actor-critic triage & categorization | Completed | `SciuroIngestionOrchestrator`, basic static heuristic engine, inbox routing |
| B1 - Recurring obligation & debt auto-detection | Completed | `ObligationDetectionEngine` to scan ledger for recurring merchant patterns |
| B1.5 - Obligation Module Activation | Completed | Wired `obligationsModule` in Koin DI. Added repository CRUD and reactive observe. Created `ObligationCycleMatcher` for per-transaction cycle settlement. Wired into ingestion orchestrator. |
| B2 - Transfer detection | Completed | Original `TransferDetectionEngine` with amount+time heuristic (superseded by Acct+Transfer phases) |
| B3 - Balance & reconciliation engine | Completed | `ReconciliationEngine` and `CashAdjustment` schemas to fix ledger drift |
| B4 - Manual Review Inbox | Completed | Exposed `observeUnreviewedTransactions` Flow for UI consumption |
| B5 - Debt Ledger module | Completed | `core-debt` scaffolded, `DebtEngine` implemented for automatic payment tracking |
| B5.5 - Debt Module Completion | Completed | Added direction/status/counterparty to debt model. Created `feature-debt` module with full CRUD UI (view, add, edit, delete, record payment). Fixed DebtEngine direction matching for OWED_TO_ME debts. Fixed DashboardViewModel Net Position sign for receivable debts. |
| B6 - Investment/Gold Savings module | Completed | `core-investment` scaffolded, tracks asset accumulation independently |
| B7 - Budgeting logic | Completed | `core-budget` scaffolded, `BudgetEngine` implemented for rolling 30-day tracking |
| B7.5 - Budget Feature Completion | Completed | Full budget CRUD (create/edit/delete), category name resolution via `CategoryRepository`, creation bottom sheet with category picker/period selector, edit/delete flows with destructive confirmation |
| B7.6 - Budget Enhancement Round 2 | Completed | Calendar-month alignment (MONTHLY budgets), transfer-link exclusion from spend, per-budget alert threshold with three-state visual (HEALTHY/APPROACHING/OVER), rollover carry-over, BudgetThresholdCrossed event on DomainEventBus |
| B7.7 - Budget Enhancement Round 3 | Completed | Unified threshold via SettingsProvider, category icons/colors on budget cards, daily allowance + days remaining metrics, input validation (0 < amount ≤ 100,000), rollover indicator, edit dialog respects actual period, `mapCategoryIcon()` helper |
| C1 - Kanban board | Completed | `feature-kanban` UI scaffolded with Jetpack Compose and ViewModels |
| C1.5 - Kanban Unification | Completed | Added Review/Bills/Debts tabs to KanbanScreen. Bills column with status derivation (Overdue/Due Soon/Upcoming). Debts column with progress bars and payment recording. KanbanTaskCard refactored from dead code into active use. Hero panel tab-aware. |
| C4 - Runway / Cash Flow Forecast | Completed | IncomeRecurrencePatternDetector, Runway metric on Dashboard (liquid balances + income - upcoming obligations/debts), "based on bills only" caveat |
| X1 - Engine Activation | Completed | Activated `InvestmentEngine` and `ObligationDetectionEngine` in orchestrator fan-out. Cleaned up inline FQ types in orchestrator. Added `:core-investment` dep to classifier. |
| X2 - Transfer Exclusion (Debt/Investment) | Completed | Applied `transferTxIds` exclusion to `DebtEngine` and `InvestmentEngine` (matching `BudgetEngine`'s existing pattern). Prevents self-transfer false matches. |
| X3 - SettingsProvider Relocation | Completed | Moved `SettingsProvider` + `LlmParsingConfig` from `core-parsing` to `core-ledger`. Dropped extraneous `:core-parsing` edges from dashboard, wallet, budgets, and kanban. |
| X4 - Domain Event Bus Completion | Completed | Wired event publishers in `DebtEngine`, `ObligationCycleMatcher`, `ObligationDetectionEngine`. Added `KanbanViewModel` event subscription with animation trigger flow. KanbanScreen shows settle animation on bill/debt cards (UAT 465/525). |
| C2 - Home dashboard & Wallet screen | Completed | Scaffolded `feature-dashboard` and `feature-wallet` UI with ViewModels |
| C3 - Drilldown screens | Completed | Scaffolded `feature-budgets` UI and assembled app Navigation |
| D1 - Security hardening | Completed | Disabled Android auto-backup for the database domains. Added optional biometric/PIN `BiometricGate` with user-facing toggle (Settings > Security). Gate observes `ProcessLifecycleOwner` to re-prompt after 30s of backgrounding. Falls back to a "Set up device security" prompt when no screen lock is enrolled. |
| D2 - Full test pass & dogfood | Completed | Connected UI ViewModels to real SQLDelight reactive flows |
| D3 - Personal deployment | Completed | Sideloaded APK built with multiple architecture support |
| D4 - Initial Setup & Onboarding | Completed | Compose navigation for initial Personal Wallet setup and account soft-deletion schemas |
| E1 - UI/UX Modernization | Completed | Standardized UI wrappers, 3-way Appearance theming, FastTransactionSheet numpad workflow, Kanban sticky filter |
| A3.5 - Reliability Hardening | Completed | Fault isolation (per-event try/catch, restart), durable capture (RawEventStaging table), direction bug fix (nullable, inflow keywords, confidence scoring), LLM hardening (HttpTimeout, retry, circuit breaker, externalized config), OEM resilience (lifecycle hooks, WorkManager reconciliation, explicit manifest) |
| DT1-7 - Developer Tools Enhancement | Completed | 5-tab DeveloperSettingsScreen (Simulator, Sources, Ingestion Log, Diagnostics, Data Tools), SimulationEngine with per-rule diagnostics, FixtureLibrary (31 fixtures shared between tests and UI), dynamic package+template picker, dead-letter viewer, LLM debug panel, ParserTestCase moved to commonMain |
| **Acct-1–3 — Account Data Enrichment** | **Completed** | Extended account schema with number, holder name, bank code, QR image. Added edit bottom sheet and QR capture/display on detail screen. Created `account_pair_confirmation` table for tracking human-confirmed transfer pairs. |
| **Transfer-1–2 — Deterministic Self-Transfer Detection** | **Completed** | Two-tier detection: identity-based matching via counterparty account number (Tier 1) with heuristic fallback (Tier 2). Per-transaction reactive engine replaces batch scan. Masked-suffix matching for partial/masked account numbers. |
| **Hero-1–6 — Hero Panel Enhancement** | **Completed** | Added `content` slot to `HeroPanel`. Dashboard uses real balance history chart instead of mock data. Budgets hero shows allocated-vs-spent totals with at-risk mini-rows. AccountDetail absorbs Adjust button into slot. DeveloperSettings shows pipeline health (last capture time + ingestion counts). Kanban shows column breakdown counts. |
| F1 — Configurable allowlist & parser health | Completed | Runtime-editable allowlist via `MutableIngestionAllowlist` + `SettingsProvider`. Enabled "Add Custom Package" in `DeveloperTabSources`. Added `ParserHealthRepository` + `DeveloperTabHealth` tab with per-package match-rate monitoring. |
| F2 — DomainEvent catalog completion | Completed | Extended `DomainEvent` sealed interface from 5 to 23 event types matching automation cascade plan §3. No publishers or consumers changed — unblocks P3/P4. |
| F3 — Rule Learner, CategoryResolver, BudgetLimitSuggester | Completed | Migration `5.sqm` with `merchant_category_rule` + `recipient_rule` tables. `RuleLearner` subscribes to event bus and learns merchant→category. `CategoryResolver` replaces inline `guessCategoryId()`. `BudgetLimitSuggester` with 90-day trimmed mean. Budget creation sheet shows suggestion chip. `BudgetLimitSuggesterTest` (8 tests): null guards, trimmed mean, outlier trimming, lookback filtering, event publication. |
| F4 — Auto-confirm, event publishers, OEM guidance | Completed | `ConfidenceTracker` for merchant trust, `ObligationDetectionEngine` auto-confirm, `TransferDetectionEngine` publishes `TransferMatched`/`TransferUnmatchedFlagged`, `IncomeRecurrencePatternDetector` publishes via `detectAndPublish()`, `OemAutostartHelper`, auto-confirm toggle in Settings. |
| F5 — Investment PriceProvider + Valuation Engine | Completed | `PriceProvider` interface + `ManualPriceProvider` (user-set). Migration `6.sqm` for `unit_type`. `InvestmentValuationEngine` with `getTotalCurrentValue()`. `InvestmentEngine` publishes `InvestmentTransactionRecorded`. |
| F6 — Debt payment link table | Completed | Migration `7.sqm` + `DebtPaymentLinkRepository`. `DebtEngine` rewritten to use link-based matching with `UNIQUE(debt_id, transaction_id)` for structural idempotency. Prevents double-counting. |
| F7 — Encrypted export & reconciliation worker | Completed | `EncryptedExporter` (AES-GCM + PBKDF2), `EncryptedImporter` (decrypt + restore), `IngestionReconciliationWorker` (6h periodic). "Data Backup" card in Settings. |
| F8 — Net Position rollup | Completed | `NetPositionSubscriber` recomputes net worth from accounts + cash + investments − debts on each bus event. Proves "derive, don't duplicate" architecture. |
| **HS-1–3 — Hero Section Refactoring & Full-Screen Swiping** | **Completed** | Fixed number overflow by switching hero figure typography to `headlineLarge`. Refactored Kanban, Wallet, and Dashboard screens to use a single root `LazyColumn` pattern for full-screen swiping (hero scrolls off, sheet fills viewport). Standardized `SheetList` modifier to `fillParentMaxHeight()` across all screens. Replaced nested `LazyColumn` instances with `Column` + `forEach` inside `SheetList`. FAB moved into `Box` overlay for consistent z-ordering. |
| G1 — Polish & Reliability Gap Closure | Completed | SMS ingestion adapter (`SmsSourceAdapter` + `SmsReceiver`), `MultiplexIngestionSource` composite adapter, `NewFinanceAppDetected` package install observer (`FinanceAppInstallReceiver` + `FinanceAppCheckWorker` + `FinanceAppSuggestionSubscriber`), battery optimisation exemption (onboarding step + Settings card, moved `OemAutostartHelper` to `core-ui`, fixed Oppo/Realme bug), cash adjustment remarks with anomaly-aware prompts (migration `9.sqm`), quiet hours toggle with runway-aware `NotificationSuppressionEngine` and budget alert notifications. Deferred: Lottie mascot, pull-to-refresh, shared-element transitions. |
| G2 — Domain Event & Automation Completion | Completed | Published `CashRecounted`, `ObligationAmountDrifted`, `BnplRiskThresholdCrossed` events. Created `UniversalEventSubscriber` converting 9 domain events to system notifications. Created `BnplRiskDetector` for BNPL/pay-later risk monitoring. Added `YahooFinancePriceProvider` with live gold (metals.live) + stock (Yahoo Finance) pricing and USD→MYR conversion. Wired `InvestmentValuationEngine` into `WalletScreen` for live portfolio valuation. Encrypted export/import UI buttons in Settings > Data Backup. `CreditCardStatementEngine` for statement cycle tracking. Budget-6 `CategoryDrilldownScreen` with per-category spend breakdown and budget progress bars. Android 13+ runtime receiver registration for `FinanceAppInstallReceiver`. Created missing TEST_NOTES.md for A3.5, F7, F8. |
| N1 — Notification coverage | Completed | Parser corrections (BSN DuitNow keywords, Maybank legacy m2u + title fallback, CIMB OCTO). Aggregator forward matching for forwarded-payment emails. Cross-channel dedup via `transaction_corroboration` + `findLikelyDuplicate`. Tight-match auto-confirm for 15-second self-transfers. `LinkedAccountsScreen` manual pair seeding. LLM-fallback candidate logging via developer diagnostics. |
| I1 — Budget UX/UI Enhancement | Completed | Comprehensive UX/UI audit and polish of budget module. Removed non-destructive confirmation dialogs (create/save). Added snackbar feedback after mutations. Fixed hardcoded title. Wired CategoryDrilldownScreen navigation with back button. Replaced nested LazyColumn with Column+forEach. Fixed FAB visibility in dark mode. Added inline amount validation. Added period badges to budget cards. Removed fake pull-to-refresh. Standardized progress bar heights. |
| **J1 — Developer Options Enhancement** | **Completed** | Comprehensive enhancement of developer settings. Migrated 40+ hardcoded strings to resources. Fixed Koin dependency injection in tab composables (3 files). Added deep link support for tab navigation. Added developer options gating (hidden by default, 7-tap activation). Added error handling with Snackbar feedback. Added two-step delete confirmation for Clear Inbox. Added determinate batch progress bar. |
| **K1 — UI Polish & Accessibility Pass** | **Completed** | Cross-cutting polish across all modules. Extracted 200+ hardcoded strings to strings.xml (6 new + 1 extended resource files across feature-dashboard, feature-wallet, feature-kanban, feature-debt, feature-budgets, feature-settings, core-ui). Added PillToggle accessibility semantics (role=Tab, selected state). Fixed hardcoded contentDescription in KanbanScreen. Created @Preview composables for all 7 screens. Fixed core-ui compile error (contentPadding), preview parameter mismatches, JdbcSqliteDriver imports in 4 test files, TransactionMatchingEngine constructor. Fixed detekt issues (LongParameterList, UnusedParameter, UnusedPrivateMember). |
| **R1 — Reliability & Performance Hardening** | **Completed** | Atomic DB transactions (all TransactionRepository mutations), SQL-optimized budget queries (indexed SUM instead of full table scan), engine debouncing (15s cooldown on full-scan engines), SmsReceiver goAsync() lifecycle fix, Tier 0 transfer detection via confirmed pairs, navigation extraction (SciuroRoute sealed class + SciuroNavGraph), ProGuard rules for SQLCipher/Koin/Ktor/serialization. |
| **R2 — Engine Trigger Persistence & Concurrency Hardening** | **Completed** | Made the 7-engine debounce durable: `DefaultEngineTriggerUseCase` replaced in-memory `var lastXxxRunMs` fields with persisted timestamps (`SettingsProvider.get/setEngineLastRunMs`, `EncryptedSharedPreferences` keys `engine_last_run_*`), eliminating the crash-restart thundering-herd of full-scan engines. Added a debounce `Mutex` guarding atomic check-and-reserve plus a per-engine `Mutex` so concurrent events can never invoke the same engine twice. Engine classes made `open` for testability; added `DefaultEngineTriggerUseCaseTest` (7 engines, debounce, persistence-survival, failure isolation, concurrent calls). See [phase-R2-engine-trigger-persistence/TEST_NOTES.md](phase-R2-engine-trigger-persistence/TEST_NOTES.md). |
| **N2 — User-Configurable Notification Preferences** | **Completed** | `NotificationPreferencesStore` (EncryptedSharedPreferences-backed, 13 channel enable/disable toggles + typed params). 8 new Android notification channels (Backup, Runway, Spending, Digest, BNPL, Cash, Transfer, Milestone). P4 fixes: BNPL/Cash/Transfer use proper channels instead of `showBudgetAlert()` hijack. Consolidated `NightlyCheckWorker` (daily: backup, runway, debts, income, digest). `NetPositionMilestoneReached` event + milestone detection. Settings UI with 5 grouped cards and expandable sliders. All notifications gated by user preferences. |
| **J2 — Developer Options & Settings Redesign** | **Completed** | Replaced `ScrollableTabRow` with `PillToggle(scrollable=true)` in `DeveloperSettingsScreen`. Redesigned all 7 developer tabs with consistent `SciuroCard` wrapping, `RoundedCornerShape(16.dp)` consolidation, and structural improvements. Added `renamePackage()` to `MutableIngestionAllowlist` with edit UI (pencil icon + `SciuroFormSheet`). Restructured `SettingsScreen` to `Box(fillMaxSize)` + `LazyColumn` full-screen swiping pattern: `HeroPanel` scrolls with content, `SheetList` uses `fillParentMaxHeight()` (no nested `LazyColumn`). Extracted NotificationsSection composable. Fixed `importFilePickerLauncher` scoping, battery state lifecycle. |
| **S1 — Database Safety: Key-Loss Detection & Quarantine** | **Completed** | Replaced `catch (Exception) { deleteDatabase }` in `PlatformDatabaseModule` with two-phase startup that quarantines the database on key-loss or corruption instead of deleting it. Added `passphraseExists()`, `getStoredPassphrase()`, `storePassphrase()` to `DatabaseKeyManager` for passphrase lifecycle management. Bumped encrypted backup format to version 2 — includes the SQLCipher passphrase in the payload, enabling automatic key recovery on import. |
| **S2 — Kanban/Debt/Obligation Edge Case Fixes** | **Completed** | Fixed 12 edge cases across kanban/obligation/debt: bidirectional merchant matching, manual mark-as-paid advances obligation, debt payment clamps to zero + auto-PAID_OFF, updateDebt preserves progress/status, orphaned payment link cleanup, audit wrappers on fast-path obligation mutations, try/catch error handling on all write paths. Changed debt filter to tri-state PillToggle (Active/+Paid Off/All). Widened FrequencyDetector classification ranges to eliminate detection gaps. |
| **S3 — Database Recovery Interstitial & Weekly Integrity Checks** | **Completed** | Replaced the silent empty-database launch with a full-screen recovery interstitial when a `sciuro.db.quarantined.*` database exists: the user can Import a backup (reuses `EncryptedImporter` v1/v2) or Start fresh (quarantined files preserved), with both paths acknowledging the recovery and restarting. Added `quarantine_count`/`last_quarantine_timestamp`/ack + integrity metadata in `EncryptedSharedPreferences`, a "Database Recovery" card in the Developer Health tab, and a weekly `PRAGMA integrity_check` in `NightlyCheckWorker` traced to `pipeline_trace` (`DATABASE_INTEGRITY` stage). Startup validation now runs eagerly in `SciuroApp` before Koin so the gate can see the very launch that detects the quarantine; ingestion/workers are gated while recovery is pending. 13 core-ledger JVM tests passing. See [phase-S3-database-recovery-ui/TEST_NOTES.md](phase-S3-database-recovery-ui/TEST_NOTES.md). |
| **Core-Classifier Edge-Case Hardening (Phases 1–4)** | **Completed** | Comprehensive edge-case audit of the `core-classifier` module. Resolved 16 edge cases across 6 categories: input validation (NaN amounts, blank merchants/packages, locale-dependent lowercase), state/timing (cancellation-stranding, duplicate processing, false dedup), user behavior (account deletion orphaned rules), failure/degradation (parser null retry, infinite source reconnect), and undocumented behavior (threshold duplication, reviewTier string storage). Phase 1: guard clauses (6 fixes). Phase 2: status-based re-processing prevention. Phase 3: account deletion cascade cleanup. Phase 4: dedup merchant criterion, parser retry, max reconnects. All 24 classifier tests passing. |
| **M1 — Merchant Rules UI (Dark Component Gap Closure)** | **Completed** | Created a dedicated Merchant Rules settings screen (`feature-settings`) surfacing six previously-invisible backend components. **ConfidenceTracker** + **RuleLearner** now exposed as a navigable list of learned merchant→category mappings with confirmation counts, trust badges (green check at ≥3), delete/reset, and category override via bottom sheet. Data layer: `MerchantRuleRepository` (`core-ledger`) wraps `merchantCategoryRuleQueries` with reactive Flow observation via `combine(asFlow, categories)`. Entry point: Settings → Data & Privacy → Manage Merchant Rules (sibling to Manage Categories). |
| **M2 — Stale Data Display (4 items)** | **Completed** | Extended 4 existing screens with data that was already computed but never surfaced. **InvestmentCard live pricing** — per-investment live values from Yahoo Finance with green/red gain indicators, refreshed on pull-to-refresh. **BNPL risk card** — amber warning on DebtOverviewScreen when ≥2 active BNPL/credit card debts. **Credit card statement** — statement summary (balance, min payment, cycle payments, days remaining) in DebtDetailSheet for CREDIT_CARD debts. **Income pattern row** — Dashboard runway card shows "Next: RM X on [date]" when recurring income is detected. |
| **M3 — State Tracking (3 items)** | **Completed** | Added in-app visibility for ephemeral domain events. **Obligation drift badge** — Kanban bill cards show "AMOUNT CHANGED" badge when `ObligationAmountDrifted` fires; BillDetailSheet shows old→new amounts. **Transfer candidate badge** — unmatched self-transfer candidates labeled "TRANSFER" in Kanban Review tab. **Net worth milestone** — Dashboard hero panel shows "RM X milestone reached" badge from persistent settings. |
| **M4 — New UI Surface (3 items)** | **Completed** | Created 3 new UI surfaces. **Manual price override** — `InvestmentPriceScreen` in feature-settings lists all investments with book/live/manual prices and Set/Clear buttons. **Weekly digest card** — real-time 7-day spending summary on Dashboard. **Merchant→account rules tab** — PillToggle on MerchantRulesScreen adds Accounts tab showing learned merchant→account associations with delete. |
| **G4 — Domain Event Reliability** | **Completed** | Durable domain event delivery with per-subscriber acknowledgment. `DomainEventEnvelope` model, `DomainEventStore` interface, `DomainEventCodec` for serialization of all 27 event types. SQLDelight tables `domain_event_log` (immutable event log) and `domain_event_delivery` (per-subscriber delivery state). `SqlDelightEventStore` implementation. `DomainEventBus` enhanced with durable persistence before live emission, `metrics()` API, and `subscribe()` for durable delivery. Developer Health tab shows pending deliveries, dead letters, retries, live drops, and subscriber lag. Schema migration 15.sqm. 30 tests across codec, store, and bus. |
